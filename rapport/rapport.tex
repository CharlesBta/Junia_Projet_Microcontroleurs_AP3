\documentclass{report}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{color}
\usepackage{xcolor}
\usepackage{soul}
\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{tikz}
\usepackage{longtable}


\newcommand{\DocName}{Projet Microcontrôleurs - Rapport}
\newcommand{\DocAuthors}{\bsc{Da Silva} Tristan - \bsc{Battaglia} Charles-Antoine \\ \bsc{Botté-Magalhaes} Jules - \bsc{Gauthier} Keanu}

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}
\definecolor{addrred}{rgb}{0.93,0.43,0.43}
\definecolor{commentgray}{rgb}{0.45,0.45,0.45}

\lstdefinelanguage{MicroAsm}{
    sensitive=true,
    morecomment=[l]{\#},
    moredelim=[s][\color{addrred}]{@}{:}
}

\lstdefinelanguage{HexProg}{
    sensitive=true,
    morecomment=[l]{;}
}

\lstdefinelanguage{AsmStd}{
    sensitive=true,
    morecomment=[l]{;},
    morecomment=[l]{\#},
    morekeywords={
        MOV,ADD,SUB,MUL,DIV,INC,DEC,CMP,AND,OR,XOR,NOT,
        JMP,JE,JNE,JG,JL,JGE,JLE,CALL,RET,PUSH,POP,
        LDA,STA,LOAD,STORE,INPUT,OUTPUT,HALT,NOP
    }
}

\lstset{frame=tb,
    language=MicroAsm,
    aboveskip=3mm,
    belowskip=3mm,
    showstringspaces=false,
    columns=flexible,
    basicstyle={\small\ttfamily},
    numbers=none,
    numberstyle=\tiny\color{gray},
    keywordstyle=\color{blue},
    commentstyle=\color{commentgray}, 
    breaklines=true,
    breakatwhitespace=true,
    tabsize=3,
   }

\lstset{breaklines=true}

\geometry{a4paper, margin=1in}

% Suppression de l'affichage du mot "Chapitre"
\titleformat{\chapter}[display]
    {\normalfont\bfseries\LARGE}
    {}
    {0pt}
    {}

\definecolor{Light}{gray}{.90}
\sethlcolor{Light}

\let\OldTexttt\texttt
\renewcommand{\texttt}[1]{\OldTexttt{\hl{#1}}}

\renewcommand{\thechapter}{\arabic{chapter}}
\renewcommand{\thesection}{\thechapter.\arabic{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}

\fancyhf{}
\fancyhead[L]{\DocName}
\fancyfoot[L]{\DocAuthors}
\fancyfoot[R]{\thepage / \pageref{LastPage}}
\renewcommand{\headrulewidth}{0 pt}
\renewcommand{\footrulewidth}{0.2pt}
\pagestyle{fancy}
\fancypagestyle{plain}{
    \fancyhf{}
    \fancyfoot[R]{\thepage / \pageref{LastPage}}
    \fancyfoot[L]{\DocAuthors}
    \fancyhead[L]{\DocName}
}

\graphicspath{ {./img} }

\title{\DocName}
\author{\DocAuthors}
\date{\textbf{JUNIA - ISEN - AP3} \url{https://github.com/CharlesBta/Junia_Projet_Microcontroleurs_AP3.git}}

\newcounter{questioncounter}
\setcounter{questioncounter}{0}

\begin{document}
\maketitle
\tableofcontents

\chapter{Analyse fonctionnelle du circuit}

\section{Présentation générale du circuit}

Le circuit étudié correspond à une carte électronique de vumètre audio. Sa fonction est de recevoir un signal audio stéréo, de le filtrer, de l'analyser selon plusieurs bandes de fréquence, puis de faire allumer des LEDs en fonction du niveau sonore détecté. Le montage peut être découpé en plusieurs parties : l'entrée audio, la préamplification, la génération d'une tension de référence, le mélange des entrées droite et gauche, le filtrage analogique, la détection d'enveloppe, le traitement par microcontrôleur, puis la commande des LEDs, et de la matrice LED.

\section{Entrée du signal audio}

Le signal audio entre dans la carte par le connecteur jack d'entrée \texttt{J\_JACK\_IN1}. Ce connecteur permet de récupérer les deux voies d'un signal audio stéréo : la voie droite, notée \texttt{AUDIO\_R}, et la voie gauche, notée \texttt{AUDIO\_L}. Ces deux signaux sont ensuite dirigés vers les premiers étapes analogiques du montage.

Comme un signal audio est un signal alternatif, il peut naturellement prendre des valeurs positives et négatives. Or, la carte est alimentée uniquement entre \(+5\,\mathrm{V}\) et \(\mathrm{GND}\). Il est donc nécessaire d'adapter ce signal pour qu'il reste compatible avec les composants alimentés en simple alimentation, notamment les amplificateurs opérationnels (AOP) et le microcontrôleur.

\section{Préamplification des voies audio}

Avant d'être utilisé, le signal audio est traité par une étape de préamplification réalisée autour d'AOP \texttt{MCP6274}. Les deux voies audio passent notamment par les amplificateurs \texttt{OPA1D} pour la voie droite et \texttt{OPA1C} pour la voie gauche.

Ces étages permettent d'adapter le niveau du signal audio et de fournir un signal plus exploitable pour les blocs suivants. Les résistances de contre-réaction, telles que \texttt{Rpre1} et \texttt{Rpre3}, contribuent à fixer le gain de ces amplificateurs. Cette étape ne vise pas uniquement à amplifier le signal, mais également à l'adapter à une plage de tension appropriée pour les traitements analogiques.

\section{Génération de la tension de référence}

Pour permettre ce traitement avec une alimentation simple, le montage crée une tension de référence appelée \texttt{Vref}. Cette tension est obtenue à partir d'un pont diviseur de tension constitué notamment de \texttt{Rref1} et \texttt{Rref2}, puis elle est stabilisée par un AOP monté en suiveur.

Cette tension de référence joue le rôle de masse virtuelle. Elle permet de centrer les signaux audio autour d'une valeur intermédiaire de \(2{,}5\,\mathrm{V}\), au lieu de les centrer autour de \(0\,\mathrm{V}\). Ainsi, la tension oscille entre \(0\,\mathrm{V}\) et \(5\,\mathrm{V}\), ce qui permet au microcontrôleur de les mesurer sans recevoir de tension négative.
\newpage
\section{Mélange des voies gauche et droite}

Après préamplification, les deux voies audio gauche et droite sont regroupées dans un étage de mélange. Les signaux \texttt{OUT\_PREAMP\_L} et \texttt{OUT\_PREAMP\_R} arrivent sur un montage réalisé autour de l'amplificateur \texttt{OPA1B}. À travers plusieurs résistances d'entrée, les deux signaux sont additionnés pour former un signal unique appelé \texttt{OUT\_MIX}.

Cet étage transforme donc le signal stéréo en signal mono. Cette opération est pertinente dans le cadre d'un vumètre, puisque l'objectif n'est pas de reproduire distinctement les deux canaux audio, mais de fournir une indication lumineuse globale du niveau sonore.

\section{Filtrage analogique du signal}

Le signal mélangé \texttt{OUT\_MIX} est ensuite envoyé vers plusieurs filtres analogiques. Ces filtres sont réalisés à l'aide d'AOP, de résistances et de condensateurs. Le schéma présente quatre sorties filtrées : \texttt{OUT\_FILTER1}, \texttt{OUT\_FILTER2}, \texttt{OUT\_FILTER3} et \texttt{OUT\_FILTER4}.

Leur rôle est de séparer le signal audio en différentes bandes de fréquence. Chaque filtre extrait donc une partie spécifique du signal : par exemple une bande correspondant aux basses fréquences, une autre aux fréquences intermédiaires, et d'autres aux fréquences plus élevées. Les valeurs des résistances et des condensateurs déterminent les fréquences de coupure de chaque filtre. Grâce à cette étape, le vumètre peut réagir différemment selon les fréquences du signal audio, plutôt que d'afficher uniquement un niveau sonore global.

\section{Détection d'enveloppe}

Les sorties des filtres sont ensuite envoyées vers des détecteurs d'enveloppe. Ces détecteurs sont réalisés avec des diodes \texttt{1N4148}, des résistances et des condensateurs. On obtient alors les sorties \texttt{OUT\_ENV\_1}, \texttt{OUT\_ENV\_2}, \texttt{OUT\_ENV\_3} et \texttt{OUT\_ENV\_4}.

Le détecteur d'enveloppe transforme le signal audio alternatif en une tension plus lente, représentative de son amplitude. La diode redresse le signal, tandis que le condensateur se charge aux pics de tension puis se décharge progressivement dans la résistance parallèle.

La vitesse de décharge dépend de la constante de temps RC. Celle-ci doit être suffisamment grande pour que la tension ne redescende pas trop vite entre deux variations du signal. Ainsi, les LEDs restent allumées assez longtemps pour éviter un effet de clignotement visible par l'\oe{}il humain.

\section{Traitement par le microcontrôleur}

Les tensions issues des détecteurs d'enveloppe sont ensuite appliquées aux entrées analogiques du microcontrôleur \texttt{PIC18F25K40}. Le microcontrôleur réalise la conversion analogique-numérique des signaux \texttt{OUT\_ENV\_1} à \texttt{OUT\_ENV\_4}. Il transforme donc les niveaux de tension en valeurs numériques exploitables par programme.

Dans notre programme, les quatre tensions sont lues sur les entrées \texttt{AN5} à \texttt{AN2}. Une calibration est faite au démarrage afin de mesurer le niveau de repos de chaque bande. Ensuite, le programme moyenne les mesures, retire le niveau de repos et transforme le résultat en une hauteur comprise entre 0 et 8 LEDs.

Les quatre bandes sont affichées sur la matrice par groupes de deux colonnes. La montée de la barre est immédiate, tandis que la descente se fait une LED à la fois afin d'obtenir un affichage plus lisible.

\section{Commande des sorties lumineuses}

La carte possède plusieurs sorties lumineuses. Une première série correspond aux LEDs de test, nommées \texttt{LED\_0} à \texttt{LED\_7} sur le schéma. Dans notre programme, elles affichent la hauteur de la bande qui possède le niveau le plus élevé. Elles permettent donc de vérifier le traitement des mesures sans utiliser la matrice.

Une seconde sortie est dédiée à la matrice de LEDs, via le connecteur \texttt{J4 / Conn\_LEDMATRIX}. Cette matrice constitue l'affichage principal du vumètre. Les LEDs sont vertes sur les quatre premières lignes, jaunes sur les deux suivantes puis rouges sur les deux dernières.

\section{Sortie audio}

Le circuit conserve également une sortie audio via le connecteur \texttt{J\_JACK\_OUT1}. Cette sortie permet de récupérer le signal audio après passage par la carte. Elle permet donc au signal audio de traverser le montage tout en réalisant l'analyse nécessaire au vumètre. La carte ne se limite pas à la mesure du signal : elle s'intègre dans une chaîne audio grâce à une entrée et une sortie audio.

\section{Alimentation et découplage}

L'alimentation de la carte est assurée par une tension \(+5\,\mathrm{V}\) et une masse \(\mathrm{GND}\), amenées par le connecteur d'alimentation \texttt{J\_ALIM1}. Plusieurs condensateurs de découplage sont placés à proximité des différents blocs du montage, comme \texttt{CdecOPA1}, \texttt{CdecOPA2}, \texttt{CdecMCU1} ou \texttt{CdecMATRIX1}.

Leur rôle est de maintenir une alimentation stable au plus près des composants et de réduire les perturbations électriques. Ils absorbent notamment les variations rapides de courant provoquées par les AOP, le microcontrôleur ou les LEDs. Ces condensateurs permettent ainsi de limiter les parasites, d'éviter les instabilités et d'améliorer la fiabilité des mesures analogiques.

\section{Connecteur de programmation}

Enfin, le connecteur \texttt{J3} correspond au connecteur de programmation du microcontrôleur. Il permet de programmer le \texttt{PIC18F25K40} sans le retirer de la carte. Ce connecteur donne accès aux voies nécessaires à la programmation, notamment l'alimentation, la masse et les broches de communication dédiées.

Il ne participe pas directement au traitement du signal audio, mais il est indispensable pour charger le programme dans le microcontrôleur et modifier son comportement.

\section{Synthèse du fonctionnement}

En résumé, le fonctionnement global du circuit est le suivant : le signal audio stéréo entre sur la carte, les voies gauche et droite sont préamplifiées puis mélangées en un signal mono. Ce signal est ensuite séparé en plusieurs bandes de fréquence par des filtres analogiques. Chaque signal filtré est transformé par un détecteur d'enveloppe en une tension proportionnelle au niveau sonore de la bande correspondante.

Ces tensions sont lues par le microcontrôleur, qui les convertit en valeurs numériques et pilote ensuite les LEDs de test ainsi que la matrice de LEDs. L'ensemble du circuit est alimenté en \(+5\,\mathrm{V}\).

\newpage

\chapter{Calculs valeurs analogiques}

\section{Dimensionnement du PREAMP\_R et du PREAMP\_L}
\textit{Le dimensionnement du PREAMP\_R est identique à celui du PREAMP\_L, on ne détaille que le PREAMP\_R}
\\

On prend :
\[f_c = 20 \text{ Hz} \quad \text{et} \quad C_{pre0} = 0,68 \times 10^{-6} \text{ F}
\]

On a :
\[ f_c = \frac{\omega_c}{2\pi} = \frac{1}{2\pi R_{pre0} C_{pre0}} = 20 \text{ Hz}
\]

Donc : 
\[ R_{pre0} = \frac{1}{2\pi C_{pre0} \times 20} = \frac{1}{2\pi \times 0,68 \times 10^{-6} \times 20} = 11,7 \text{ k}\Omega
\]
\\

\textit{Pour calculer la valeur de \(R_{pre1}\) il a fallu regarder la tension aux bornes de notre entrée jack à l'aide d'un oscilloscope, pour cela on a émis un son de 1000 Hz avec un ordinateur qui avait le volume à 100\%. \\ 
Après lecture de l'oscilloscope, on a les curseurs \(A_Y = 180\,\text{mV}\) et \(B_Y = -220\,\text{mV}\).}
\\

On a : 
\[ V_{ccin} = V_{pp} = V_{max} - V_{min} = 180 \text{ mV} - (-220 \text{ mV}) = 400 \text{ mV} = 0,4 \text{ V}
\]

On pose : 
\[V_{ccout} = 5 \text{ V}\]

On a : 
\[|G| = \frac{V_{ccout}}{V_{ccin}} = \frac{5}{0,4} = 12,5 
\]
Donc : 
\[ R_{pre1} = |G| \times R_{pre0} = 12,5 \times 12 \text{ k}\Omega = 150 \text{ k}\Omega
\]

On prend donc pour valeurs de composants à disposition : 
\\

\textit{Pour obtenir une résistance équivalente de \(11\,\mathrm{k}\Omega\), il faut utiliser deux résistances de \(22\,\mathrm{k}\Omega\) montées en parallèle. Pour obtenir une résistance équivalente de \(149{,}86\,\mathrm{k}\Omega\), il faut monter une résistance de \(470\,\mathrm{k}\Omega\) en parallèle avec une résistance de \(220\,\mathrm{k}\Omega\).}
\[
\fbox{$R_{pre0} = 11 \text{ k}\Omega$}
\]
\[\fbox{$R_{pre1} = 149,86 \text{ k}\Omega$} 
\]
\[
\fbox{$C_{pre0} = 0,68 \times 10^{-6} \text{ F}$}
\]

Il faut donc recalculer la valeur de \(f_c\) avec la valeur de \(R_{pre0}\) à disposition :
\[ f_{c\text{r\'eel}} = \frac{1}{2\pi R_{pre0} C_{pre0}} = \frac{1}{2\pi \times 11 \times 10^3 \times 0,68 \times 10^{-6}} = 21,3 \text{ Hz}
\]

\newpage

\section{Dimensionnement du MIXER STÉRÉO}
On a la fonction :
\[
Mix_{L}(\alpha) = -\frac{R_{mix4}}{R_{mix0} + R_{mix2} + \frac{R_{mix0} R_{mix2}}{P_{mix0} \alpha}}
\]

On sait que :
\[P_{mix0} = 10 \text{ k}\Omega\]

\textit{On cherche à simplifier l'équation en rendant \(R_{mix0} + R_{mix2} << R_{mix0} \times R_{mix2}\)}

On pose donc :
\[R_{mix0} = R_{mix2} = R = 82 \times 10^3 \text{ }\Omega
\]

ainsi :
\[R_{mix0} + R_{mix2} = 164 \text{ k}\Omega
\]
\[R_{mix0} \times R_{mix2} = 6724 \text{ k}\Omega^2
\]

On a donc :
\[R_{mix0} + R_{mix2} << R_{mix0} \times R_{mix2}
\]

Alors on peut simplifier l'équation :
\[Mix_L(\alpha) \approx -\frac{R_{mix4} P_{mix0} \alpha}{R^2}
\]

On cherche à se rapprocher d'une fonction linéaire de gain 1, soit :

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[scale=4]
        \draw[->] (0,0) -- (1.2,0) node[right] {$\alpha$};
        \draw[->] (0,0) -- (0,1.2) node[above] {Volume};

        \foreach \x in {0,0.5,1}
            \draw (\x,0.05) -- (\x,-0.05) node[below] {\x};
        \foreach \y in {0,0.5,1}
            \draw (0.05,\y) -- (-0.05,\y) node[left] {\y};

        \draw[green] (0,0) -- (1,1);

        \filldraw (0.5,0.5) circle (0.5pt);
    \end{tikzpicture}
    \caption{Fonction linéaire de gain gauche}
    \label{fig:mixer_linear_left}
\end{figure}

On cherche donc à trouver la valeur de \(R_{mix4}\) qui nous permettra d'avoir une fonction linéaire de gain, soit :
\[\frac{1}{2} = -\frac{R_{mix4} P_{mix0}}{R^2} 
\]
\[\frac{\frac{1}{2} \times 82^2}{10 \times 10^3 \times \frac{1}{2}} = R_{mix4} \approx  672,4 \times 10^3 \Omega
\]
On prend donc pour valeur de composant à disposition :
\[\fbox{$R_{mix4} = 680 \text{ k}\Omega$}
\]

\textit{Reciproquement, pour le canal droit}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[scale=4]
        \draw[->] (0,0) -- (1.2,0) node[right] {$\alpha$};
        \draw[->] (0,0) -- (0,1.2) node[above] {Volume};

        \foreach \x in {0,0.5,1}
            \draw (\x,0.05) -- (\x,-0.05) node[below] {\x};
        \foreach \y in {0,0.5,1}
            \draw (0.05,\y) -- (-0.05,\y) node[left] {\y};

        \draw[red] (0,1) -- (1,0);

        \filldraw (0.5,0.5) circle (0.5pt);
    \end{tikzpicture}
    \caption{Fonction linéaire de gain droit}
    \label{fig:mixer_linear_right}
\end{figure}

\newpage

\section{Dimensionnement des filtres}

\subsection{FILTER1}
On a :
\[f = 250 \text{ Hz}
\]
\[\omega = 2\pi f = 2\pi \times 250 = 500\pi \text{ rad/s}
\]

La fonction de transfert \( H_1(j\omega) \) est donnée par :
\[H_1(j\omega) = \frac{1}{1 + j R_0 C_0 \omega}
\]

On définit la fréquence de coupure \( \omega_c \) par :
\[
\omega_c = \frac{1}{R_0 C_0}
\]
\[
f_c = \frac{\omega_c}{2\pi} = \frac{1}{2\pi R_0 C_0}=250 \text{ Hz}
\]

On fixe :
\[C_0 = 47 \times 10^{-9} \text{ F}
\]
\[R_0 = \frac{1}{2\pi \times 250 \times 47 \times 10^{-9}} = 13,5 \text{ k}\Omega
\]

On prend donc pour valeurs de composants à disposition :
\[
\fbox{$R_0 = 10 \text{ k}\Omega$}
\]
\[
\fbox{$C_0 = 47 \times 10^{-9} \text{ F}$}
\]

\newpage

\subsection{FILTER2}

On a :
\[
f = 4 \times 10^3 \text{ Hz}
\]
\[
\omega = 2\pi f = 2\pi \times 4 \times 10^3 = 8000\pi \text{ rad/s}
\]

La fonction de transfert \( H_2(j\omega) \) est donnée par :
\[
H_2(j\omega) = \frac{j R_1 C_1 \omega}{1 + j R_1 C_1 \omega}
\]

On définit la fréquence de coupure \( \omega_c \) par :
\[
\omega_c = \frac{1}{R_1 C_1} \quad \Leftrightarrow \quad \frac{\omega}{\omega_c} = R_1 C_1 \omega
\]

Donc :
\[
H_2(j\omega) = \frac{1}{1 + j \frac{\omega}{\omega_c}} \times j \frac{\omega}{\omega_c} = \frac{j \frac{\omega}{\omega_c}}{1 + j \frac{\omega}{\omega_c}}
\]

On fixe :
\[
C_1 = 6,8 \times 10^{-9} \text{ F}
\]
\[
R_1 = \frac{1}{4 \times 10^3 \times 2\pi \times 6,8 \times 10^{-9}} = 5,6 \text{ k} \Omega
\]

On prend donc pour valeurs de composants à disposition :
\[
\fbox{$R_1 = 5,6 \text{ k}\Omega$}
\]
\[
\fbox{$C_1 = 6,8 \times 10^{-9} \text{ F}$}
\]

\newpage

\subsection{FILTER3}
On a :
\[f_{c3L} = 250 \text{ Hz} \quad \text{et} \quad f_{c3H} = 1 \times 10^3 \text{ Hz}
\] 

La fonction de transfert \( H_3(j\omega) \) est donnée par :
\[H_3(j\omega) = -\frac{R_3}{R_2} \times \frac{j R_2 C_2 \omega}{1 + j R_2 C_2 \omega} \times \frac{1}{1 + j R_3 C_3 \omega}
\]

On pose: 
\[R_3 = R_2 = R
\]

Donc :
\[H_3(j\omega) = -\frac{1}{1 + j R C_2 \omega} \times j R C_2 \omega \times \frac{1}{1 + j R C_3 \omega}
\]

On a : 
\[\omega_{c2} = \frac{1}{R C_2} \quad \text{et} \quad \omega_{c3} = \frac{1}{R C_3}\]

Donc :
\[H_3(j\omega) = -\frac{1}{1 + j \frac{\omega}{\omega_{c2}}} \times j \frac{\omega}{\omega_{c2}} \times \frac{1}{1 + j \frac{\omega}{\omega_{c3}}}
\]

On fixe :
\[R = 10 \text{ k}\Omega
\]

On pose: 
\[\frac{\omega_{c2}}{2\pi} = \frac{1}{2\pi R C_2} = f_{c3L} = 250 \text{ Hz}
 \quad \text{et} \quad \frac{\omega_{c3}}{2\pi} = \frac{1}{2\pi R C_3} = f_{c3H} = 1 \times 10^3 \text{ Hz}
\]

On a donc :
\[C_2 = \frac{1}{2\pi R f_{c3L}} = \frac{1}{2\pi \times 10 \times 10^3 \times 250} = 64 \times 10^{-9} \text{ F}
\]
\[C_3 = \frac{1}{2\pi R f_{c3H}} = \frac{1}{2\pi \times 10 \times 10^3 \times 1 \times 10^3} = 16 \times 10^{-9} \text{ F}
\]

On prend donc pour valeurs de composants à disposition :
\[\fbox{$R_2 = R_3 = 10 \text{ k}\Omega$}
\]
\[\fbox{$C_2 = 68 \times 10^{-9} \text{ F}$}
\]
\[\fbox{$C_3 = 15 \times 10^{-9} \text{ F}$}
\]

\newpage

\subsection{FILTER4}
On a :
\[f_{c4L} = 1 \times 10^3 \text{ Hz} \quad \text{et} \quad f_{c4H} = 4 \times 10^3 \text{ Hz}
\] 

La fonction de transfert \( H_4(j\omega) \) est donnée par :
\[H_4(j\omega) = -\frac{R_5}{R_4} \times \frac{j R_4 C_4 \omega}{1 + j R_4 C_4 \omega} \times \frac{1}{1 + j R_5 C_5 \omega}
\]

On pose: 
\[R_4 = R_5 = R
\]

Donc :
\[H_4(j\omega) = -\frac{1}{1 + j R C_4 \omega} \times j R C_4 \omega \times \frac{1}{1 + j R C_5 \omega}
\]

On a : 
\[\omega_{c4} = \frac{1}{R C_4} \quad \text{et} \quad \omega_{c5} = \frac{1}{R C_5}\]

Donc :
\[H_4(j\omega) = -\frac{1}{1 + j \frac{\omega}{\omega_{c4}}} \times j \frac{\omega}{\omega_{c4}} \times \frac{1}{1 + j \frac{\omega}{\omega_{c5}}}
\]

On fixe :
\[R = 10 \text{ k}\Omega
\]

On pose: 
\[\frac{\omega_{c4}}{2\pi} = \frac{1}{2\pi R C_4} = f_{c4L} = 1 \times 10^3 \text{ Hz}
 \quad \text{et} \quad \frac{\omega_{c5}}{2\pi} = \frac{1}{2\pi R C_5} = f_{c4H} = 4 \times 10^3 \text{ Hz}
\]

On a donc :
\[C_4 = \frac{1}{2\pi R f_{c4L}} = \frac{1}{2\pi \times 10 \times 10^3 \times 1 \times 10^3} = 16 \times 10^{-9} \text{ F}
\]
\[C_5 = \frac{1}{2\pi R f_{c4H}} = \frac{1}{2\pi \times 10 \times 10^3 \times 4 \times 10^3} = 3,9 \times 10^{-9} \text{ F}
\]

On prend donc pour valeurs de composants à disposition :
\[\fbox{$R_4 = R_5 = 10 \text{ k}\Omega$}
\]
\[\fbox{$C_4 = 15 \times 10^{-9} \text{ F}$}
\]
\[\fbox{$C_5 = 4,7 \times 10^{-9} \text{ F}$}
\]

\newpage

\section{Dimensionnement des Détecteurs d'enveloppe}
\textit{Pour dimensionner les détecteurs d'envoloppe, notre choix a été de prendre des valeurs pour que \({\tau_1} = R_{e1} \times C_{e1} > 20~\mathrm{ms}\). Sachant que tous les détecteurs d'enveloppe ont des valeurs égales.}
\\

Pour cela, nous avons utilisé un tableau Excel afin de comparer l'ensemble des valeurs de résistances et de condensateurs disponibles en salle de TP :
\\

\includegraphics[scale=0.22]{excel.png}

Donc nous avons pris comme valeurs : 
\[
\tau_1 = R_{e1} \times C_{e1} = 4,7 \times 10 = 47~\mathrm{ms}
\]
Avec : 
\[
\fbox{$R_{e1} = 4{,}7\,\mathrm{k}\Omega \quad \text{et} \quad C_{e1} = 10 \times 10^{-6}\,\mathrm{F}$}
\]
\\
En conclusion, nous pouvons dire qu'un \({\tau_1} = 47~\mathrm{ms}\) est pertinent étant donné que c'est suppérieur à la durée de persistance rétinienne de l'\oe{}il humain qui est de $20~\mathrm{ms}$.

\newpage

\section{Dimensionnement des Résistances de référence}

\textit{L'objectif est d'obtenir une tension de référence \(V_{ref}\) égale à \(2{,}5\,\mathrm{V}\), afin que le signal oscille entre \(0\,\mathrm{V}\) et \(5\,\mathrm{V}\). Pour cela, on réalise un pont diviseur de tension avec deux résistances de même valeur.}
\\

Donc on a : 

\[
\fbox{$R_{ref1} = R_{ref2} = 47\,\mathrm{k}\Omega$}
\]

\newpage

\section{Dimensionnement des Boutons poussoirs}
On a :
\[
I = \frac{V_{cc}}{R_B} \quad \text {et} \quad V_{cc} = 5\,\mathrm{V}
\]
\\

\textit{\(R_{B0}\) et \(R_{B1}\) dépendent de la valeur du courant I}
\\

On fixe : 
\[
I = 0{,}5\,\mathrm{mA} 
\]
Donc on a : 
\[
R_B = \frac{V_{cc}}{I} = \frac{5}{0,5 \times 10^{-3}} = 10 \,\mathrm{k}\Omega
\]
\\

On peut en conclure que : 
\[
\fbox{$R_{B0} = R_{B1} = 10 \,\mathrm{k}\Omega$}
\]

\newpage

\chapter{Programmation du microcontrôleur}

\section{Répartition entre le C et l'assembleur}

Le langage C est utilisé pour la configuration du microcontrôleur, l'acquisition et le traitement des données. L'assembleur est utilisé pour la communication avec la matrice de LEDs, car le protocole impose un timing trop précis pour être réalisé simplement en C.

On a donc séparé le programme en deux fichiers :
\begin{itemize}
    \item \texttt{main.c} contient la configuration du PIC, la lecture des quatre détecteurs d'enveloppe, le calcul des niveaux ainsi que la préparation de l'affichage.
    \item \texttt{tx.asm} contient la fonction \texttt{TX\_64LEDS}, qui envoie les 2048 bits de commande à la matrice.
\end{itemize}

La fonction assembleur est déclarée dans \texttt{main.c} avec le mot-clé \texttt{extern}. Le tableau contenant les couleurs des LEDs est également partagé entre les deux langages :

\begin{lstlisting}[language=C]
extern void TX_64LEDS(void);

volatile uint8_t LED_MATRIX[256];
volatile uint8_t *pC = LED_MATRIX;
\end{lstlisting}

Du côté assembleur, le compilateur ajoute un underscore devant les noms provenant du C. On utilise donc les symboles \texttt{\_TX\_64LEDS} et \texttt{\_pC} :

\begin{lstlisting}[language=MicroAsm]
GLOBAL _TX_64LEDS
GLOBAL _pC
\end{lstlisting}

\section{Configuration du PIC18F25K40}

Le microcontrôleur fonctionne avec son oscillateur interne réglé à \(64\,\mathrm{MHz}\). La fréquence est également définie par \texttt{\_XTAL\_FREQ}, ce qui permet d'utiliser les fonctions de délai du compilateur.

Les broches sont configurées de la façon suivante :

\begin{center}
\begin{tabular}{|c|c|p{7.5cm}|}
\hline
\textbf{Broches} & \textbf{Configuration} & \textbf{Utilisation} \\
\hline
\texttt{RA2 à RA5} & Entrées analogiques & Lecture des quatre sorties des détecteurs d'enveloppe \\
\hline
\texttt{RA6 et RA7} & Entrées avec pull-up & Boutons poussoirs, avec \texttt{RA7} utilisé pour relancer la calibration \\
\hline
\texttt{RB4} & Sortie numérique & LED principale indiquant que la calibration est terminée \\
\hline
\texttt{RB5} & Sortie numérique & Signal de commande de la matrice de LEDs \\
\hline
\texttt{RC0 à RC7} & Sorties numériques & Commande des huit LEDs de test \\
\hline
\end{tabular}
\end{center}

Les registres \texttt{ANSELx} permettent de choisir les broches analogiques. Les registres \texttt{TRISx} quand à eux permettent de choisir le sens des broches. On initialise également les registres \texttt{LATx} à 0 afin que les LEDs restent éteintes pendant le démarrage.


\section{Configuration et lecture de l'ADC}

L'ADC utilise \(V_{DD}\) et \(V_{SS}\) comme références. Avec une alimentation
de \(5\,\mathrm{V}\) et une conversion sur 10 bits, une unité ADC représente
environ \(4{,}88\,\mathrm{mV}\).

L'horloge de conversion est réglée à \(F_{OSC}/64\), soit un temps
\(T_{AD}=1\,\mu\mathrm{s}\). Le temps d'acquisition est fixé à
\(8\,T_{AD}\). Après chaque changement de canal, un délai supplémentaire de
\(5\,\mu\mathrm{s}\) permet à l'entrée de se stabiliser avant la conversion.

Les quatre canaux sont rangés dans un tableau afin de parcourir les bandes
avec une boucle :

\begin{lstlisting}[language=C]
static const uint8_t CANAL_ADC[4] = {5u, 4u, 3u, 2u};
\end{lstlisting}

L'ordre va de \texttt{ENV1}, correspondant aux graves, à \texttt{ENV4},
correspondant aux aigus. Quatre conversions sont moyennées pour chaque bande
afin de réduire les variations de mesure tout en conservant un affichage
réactif.


\section{Calibration du niveau de repos}

Les détecteurs d'enveloppe peuvent fournir une faible tension résiduelle en
l'absence de signal audio. Sans correction, cette tension pourrait provoquer
l'allumage permanent de certaines LEDs.

Après un délai de stabilisation de \(250\,\mathrm{ms}\), le programme effectue
32 mesures sur chaque bande et en calcule la moyenne. Cette valeur devient le
niveau de repos propre à chaque entrée. Le choix de plusieurs mesures limite
l'influence du bruit sans ralentir sensiblement le démarrage.

Pendant la calibration, la LED principale et les LEDs de test sont éteintes.
La LED principale s'allume lorsque l'opération est terminée. Le bouton relié
à \texttt{RA7} permet de recommencer la calibration ; un délai de
\(30\,\mathrm{ms}\) assure un anti-rebond simple.

La calibration doit être réalisée sans signal audio, sinon une partie du
signal serait considérée comme le niveau de repos.


\section{Calcul de la hauteur des barres}

Pour chaque bande, le programme retire le niveau de repos mesuré pendant la
calibration. Une marge supplémentaire de 12 points ADC permet d'ignorer les
faibles fluctuations dues au bruit.

La valeur restante est convertie linéairement en une hauteur comprise entre
0 et 8 LEDs. La pleine hauteur est atteinte pour une variation de 550 points
ADC. Les constantes \texttt{MARGE\_BRUIT} et \texttt{PLAGE\_AFFICHAGE}
permettent ainsi de régler respectivement le seuil d'allumage et la
sensibilité de l'affichage.

Lorsque le niveau augmente, la nouvelle hauteur est affichée immédiatement.
Lorsqu'il diminue, la barre descend seulement d'une LED à chaque tour de
boucle. Ce choix permet de conserver une réponse rapide à la montée tout en
rendant la descente plus progressive et plus lisible.


\section{Affichage sur les LEDs de test}

Les huit LEDs de la carte affichent la hauteur de la bande possédant le niveau
le plus élevé. Pour une hauteur donnée, le programme allume le même nombre
de LEDs en partant du bas.

Cet affichage permet de vérifier rapidement l'acquisition ADC et le calcul
des niveaux, même lorsque la matrice n'est pas connectée.

\section{Création de l'affichage de la matrice}

La matrice contient 64 LEDs RGBW. Chaque LED utilise quatre octets, dans l'ordre vert, rouge, bleu et blanc. Le tableau a donc une taille de :
\[
64 \times 4 = 256 \text{ octets}
\]

Les LEDs de la matrice sont câblées par colonnes. Pour une LED située à la colonne \(x\) et à la ligne \(y\), l'indice du premier octet est :
\[
index = (8x+y)\times 4
\]

Chaque bande de fréquence utilise deux colonnes, ce qui permet d'utiliser les huit colonnes de la matrice pour les quatre bandes.

On a choisi les couleurs suivantes :

\begin{center}
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Lignes} & \textbf{Couleur} & \textbf{Octet vert} & \textbf{Octet rouge} \\
\hline
1 à 4 & Vert & 24 & 0 \\
\hline
5 et 6 & Jaune & 18 & 18 \\
\hline
7 et 8 & Rouge & 0 & 24 \\
\hline
\end{tabular}
\end{center}

Les octets bleu et blanc restent à 0. Les valeurs utilisées restent de l'ordre de quelques dizaines, comme demandé dans la consigne, afin de limiter la luminosité et le courant consommé par la matrice.

\section{Validation de la commande de la matrice}

Le signal de commande de la matrice a été observé à l'oscilloscope sur la
broche \texttt{RB5} (voir figure \ref{fig:commande_matrice}). L'oscillogramme permet de distinguer les impulsions
correspondant aux bits 0 et 1. La largeur de l'état haut est plus courte pour
un bit à 0 que pour un bit à 1, tandis que la période totale reste proche de
\(1{,}25\,\mu\mathrm{s}\).

 \begin{figure}[htbp]
     \centering
     \includegraphics[width=0.9\textwidth]{binaire.png}
     \caption{Observation des bits 0 et 1 du signal de commande sur RB5.}
     \label{fig:commande_matrice}
 \end{figure}

Les valeurs théoriques à retrouver sont un état haut d'environ
\(0{,}375\,\mu\mathrm{s}\) pour un bit à 0 et compris entre
\(0{,}75\,\mu\mathrm{s}\) et \(0{,}8125\,\mu\mathrm{s}\) pour un bit à 1.
La présence de ces deux largeurs d'impulsion confirme que la fonction
\texttt{TX\_64LEDS} transmet correctement les deux valeurs logiques attendues
par la matrice.

\newpage
\section{Validation des détecteurs d'enveloppe}

Les sorties des détecteurs d'enveloppe ont également été observées à
l'oscilloscope (voir figure \ref{fig:detecteurs_enveloppe}). La tension obtenue suit les variations d'amplitude du signal
audio sans reproduire ses oscillations rapides. Le condensateur se charge
lorsque l'amplitude augmente, puis se décharge progressivement à travers la
résistance.
Dans un soucis de lisibilité, la sinusoïde jaune a etait inversée car elle passe par un filtre inverseur.

 \begin{figure}[htbp]
     \centering
     \includegraphics[width=0.9\textwidth]{enveloppes.png}
     \caption{Observation des tensions en sortie des détecteurs d'enveloppe.}
     \label{fig:detecteurs_enveloppe}
 \end{figure}

Cette observation confirme que les détecteurs transforment les signaux
filtrés en tensions lentes représentatives de leur amplitude. Ces tensions
peuvent ainsi être acquises par l'ADC du microcontrôleur. La décharge
progressive observée est cohérente avec la constante de temps choisie :
\[
\tau = R_e C_e = 47\,\mathrm{ms}.
\]

\newpage

\chapter{Tableau récapitulatif des composants utilisés}

\small
\renewcommand{\arraystretch}{1.25}

\begin{longtable}{|p{5cm}|c|c|p{5cm}|}
\hline
\textbf{Désignation} & \textbf{Quantité} & \textbf{Montage} & \textbf{Valeur / référence} \\
\hline
\endfirsthead

\hline
\textbf{Désignation} & \textbf{Quantité} & \textbf{Montage} & \textbf{Valeur / référence} \\
\hline
\endhead

B0, B1 & 2 & Traversant & 1825910-6 Tact. Switch \\
\hline
C0 & 1 & Traversant & \(47 \times 10^{-9}\,\mathrm{F}\) \\
\hline
C1 & 1 & Traversant & \(6{,}8 \times 10^{-9}\,\mathrm{F}\) \\
\hline
C2 & 1 & Traversant & \(68 \times 10^{-9}\,\mathrm{F}\) \\
\hline
C3 & 1 & Traversant & \(15 \times 10^{-9}\,\mathrm{F}\) \\
\hline
C4 & 1 & Traversant & \(15 \times 10^{-9}\,\mathrm{F}\) \\
\hline
C5 & 1 & Traversant & \(4{,}7 \times 10^{-9}\,\mathrm{F}\) \\
\hline
CdecMATRIX1 & 1 & Traversant & \(10\,\mu\mathrm{F}\) \\
\hline
CdecOPA2, CdecMCU1, CdecOPA1 & 3 & Traversant & \(100\,\mathrm{nF}\) \\
\hline
Ce1, Ce2, Ce3, Ce4 & 4 & Traversant & \(10 \times 10^{-6}\,\mathrm{F}\) \\
\hline
Cpre0, Cpre1 & 2 & Traversant & \(0{,}68 \times 10^{-6}\,\mathrm{F}\) \\
\hline
D0 & 1 & CMS & B340LA-13-F \\
\hline
D1, D2, D3, D4 & 4 & Traversant & 1N4148 \\
\hline
IC0 & 1 & Traversant & PIC18F25K40 \\
\hline
IC0-SOCKET & 1 & Traversant & Socket DIP 28 0.3'' \\
\hline
J\_ALIM1 & 1 & Traversant & N/R \\
\hline
J\_JACK\_IN1, J\_JACK\_OUT1 & 2 & Traversant & JYO-39-5P switched \\
\hline
J3 & 1 & Traversant & N/R \\
\hline
J4 & 1 & Traversant & N/R \\
\hline
LD1, LD5, LD3, LD7, LD2, LD4, LD0, LD6, LDM1 & 9 & CMS & LG R971 \\
\hline
OPA1, OPA2 & 2 & Traversant & MCP6274 \\
\hline
OPA1-SOCKET, OPA2-SOCKET & 2 & Traversant & Socket DIP 14 0.3'' \\
\hline
Pmix0 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
R\_LED0, R\_LED1, R\_LED2, R\_LED3, R\_LED4, R\_LED5, R\_LED6, R\_LED7, R\_LEDM1 & 9 & CMS & \\
\hline
R0 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
R1 & 1 & Traversant & \(5{,}6\,\mathrm{k}\Omega\) \\
\hline
R2 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
R3 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
R4 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
R5 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
RB0, RB1 & 2 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
Re1, Re2, Re3, Re4 & 4 & Traversant & \(4{,}7\,\mathrm{k}\Omega\) \\
\hline
RMCLR1 & 1 & Traversant & \(10\,\mathrm{k}\Omega\) \\
\hline
Rmix0, Rmix1 & 2 & Traversant & \(82\,\mathrm{k}\Omega\) \\
\hline
Rmix2, Rmix3 & 2 & Traversant & \(82\,\mathrm{k}\Omega\) \\
\hline
Rmix4 & 1 & Traversant & \(680\,\mathrm{k}\Omega\) \\
\hline
Rpre0, Rpre2 & 2 & Traversant & \(11\,\mathrm{k}\Omega\) \\
\hline
Rpre1, Rpre3 & 2 & Traversant & \(149{,}86\,\mathrm{k}\Omega\) \\
\hline
Rref1, Rref2 & 2 & Traversant & \(47\,\mathrm{k}\Omega\) \\
\hline
Visserie -- Entretoise & 4 & N/A & N/R \\
\hline
Visserie -- Vis & 4 & N/A & N/R \\
\hline

\caption{Liste récapitulative des composants utilisés dans le projet}
\label{tab:liste_composants}
\end{longtable}
\newpage

\chapter{Diagramme de Bode}
\includegraphics[scale=1.0]{bode.png}

\normalsize
\end{document}